from setuptools import setup, find_packages

setup(
    name='openoasis',
    version='0.1',
    description='OpenOakland openoasis',
    url='https://github.com/pssachdeva/openoasis',

    packages=find_packages(exclude=['contrib', 'docs', 'tests']))
